namespace LightORM;

partial class SqlFn
{
    public static bool JsonQuery(object field, string jsonPath, object value)
    {
        return default!;
    }

    public static bool JsonSet(object field, string jsonPath, object value)
    {
        return default!;
    }
}
